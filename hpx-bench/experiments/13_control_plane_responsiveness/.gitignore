# Generated run artifacts (JSONL, logs, summary.csv, manifest.json) are
# reproducible from bench.py + the bench binary. Track only the driver
# and docs; results land here and stay local unless explicitly committed.
results/
__pycache__/
*.pyc
